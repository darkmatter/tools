# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
.env
!.envrc
ops/secrets/rclone.conf

# Agenix - keep secrets.nix (public key config) but ignore decrypted secrets
secrets/*.decrypted

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
